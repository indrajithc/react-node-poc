import React from "react";
import ReactDOM from "react-dom";
import "./styles.css"; // Optional: If you plan to use CSS

const App = () => <h1>Hello, React on Node.js!</h1>;

ReactDOM.render(<App />, document.getElementById("root"));
